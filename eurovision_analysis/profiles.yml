eurovision_analysis:
  target: dev
  outputs:
    dev:
      type: snowflake
      role: SYSADMIN
      warehouse: COMPUTE_WH
      database: DEV
      schema: PUBLIC
      account: 'not needed'
      user: 'not needed'
    prod:
      type: snowflake
      role: SYSADMIN
      warehouse: COMPUTE_WH
      database: PROD
      schema: PUBLIC
      account: 'not needed'
      user: 'not needed'
